#include <iostream>
#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <gdbm.h>

int main() {
    GDBM_FILE dbf;
    std::cout << "Analitics SCRUM!\n";

    std::cout << "Access AnaliticsSCRUM DB: ... \n";
    //dbf = gdbm_open(name, block_size, flags, mode, fatal_func);
    return 0;
}